# app-flutter · flutter → Android APK

⬜ **待 W3 scaffold。**

- 栈:flutter
- 端:Android APK(证明真移动端:相机权限 + 本地存储)
- 模型类由 `../shared/skin-report.schema.json` 生成(quicktype,见 `../shared/README.md`)
- 纯 UI 复刻 `server/` 同一套 API
